<UserControl x:Class="Globe.Client.Localizer.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:platform="http://schemas.platform.com/"
        xmlns:controls="http://schemas.controls.com/"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <platform:BooleanToVisibilityConverter
            x:Key="BooleanToVisibilityConverter" 
            True="Collapsed" 
            False="Visible" />

        <Style TargetType="TextBlock" x:Key="LoginError">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="DemiBold"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Foreground" Value="Red"/>
        </Style>

    </UserControl.Resources>

    <DockPanel Width="350">
        <controls:LocPageTitleTextBlock DockPanel.Dock="Top" Text="{Binding Localize[UserLogin]}" />
        <ContentControl DockPanel.Dock="Top">
            <StackPanel>
                <controls:LocPageSubTitleTextBlock Text="{Binding Localize[Please_enter_your_credentials]}" />
                <controls:LocTextBlock Text="{Binding Localize[UserName]}" />
                <controls:LocTextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}">
                    <controls:LocTextBox.InputBindings>
                        <KeyBinding Gesture="Return" Command="{Binding LoginCommand}"></KeyBinding>
                    </controls:LocTextBox.InputBindings>
                </controls:LocTextBox>
                <controls:LocTextBlock Text="{Binding Localize[Password]}" />
                <platform:PasswordInput Name="passwordInput" Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <platform:PasswordInput.InputBindings>
                        <KeyBinding Gesture="Return" Command="{Binding LoginCommand}"></KeyBinding>
                    </platform:PasswordInput.InputBindings>
                </platform:PasswordInput>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding LoginCommand}" Content="{Binding Localize[Login]}"/>
                    <controls:LocButton Style="{StaticResource GrayButton}" Command="{Binding CancelCommand}" Content="{Binding Localize[Cancel]}"/>
                </StackPanel>
                <TextBlock Style="{StaticResource LoginError}" Visibility="{Binding Path=LoginResult.Successful, Converter={StaticResource BooleanToVisibilityConverter}}" Text="{Binding LoginResult.Error}"></TextBlock>
            </StackPanel>
        </ContentControl>
    </DockPanel>
</UserControl>